#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, m;
        cin >> n >> m;
        string s;
        cin >> s;
        vector<int >odd( m);
        vector<int >even;
        for (int i = 0; i < m; i++)
        {
            cin>>odd[i];
        }

        int cnt = 0;
        int 

        for (int i = 0; i < n; i++)
        {
            if (s[i] == 'A')
            {
                
            }
            else
            {
            }
        }
    }
    return 0;
}