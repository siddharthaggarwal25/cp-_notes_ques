#include <bits/stdc++.h>
using namespace std;
#define int long long

int solve ( int ind  , vector<int> & v ){
     
    int take  = 
}
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        vector<int> v(n);
        for (int i = 0; i < n; i++)
            cin >> v[i];
        int ans  = solve ( );
        cout<<ans<<endl;
            
    }
    return 0;
}