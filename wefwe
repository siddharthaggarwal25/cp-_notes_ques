int solve(int n, int d, vector<int>& p, vector<int>& s) {
    vector<pair<int, int>> v;
    stack<pair<double, int>> g;

    for (int i = 0; i < n; ++i) 
        v.push_back(make_pair(p[i], s[i]));

    sort(a.begin(), a.end());
    reverse( a.begin() , a.end())

    for (auto ant : a) {
        int pos = ant.first
        int  spd = ant.second;
        int temp = d- pos ;
        double t =  (double)temp / (double)spd;
        while (!g.empty() && g.top().first >= t) {
            double num  = g.top().first;
            spd = min(spd, g.top().second);
            g.pop();
        }

        g.push(make_pair (t, spd));
    }

    return g.size();
}
